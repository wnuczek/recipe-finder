# Copilot Instructions

Primary project instructions live in `AGENTS.md` at the repository root.

Copilot-specific notes:

- Follow `AGENTS.md` as the source of truth for commands, architecture, and conventions.
- Prefer concise edits aligned to existing Expo + Expo Router patterns in `app/`.
- Avoid touching `.scaffold` files unless explicitly requested.
